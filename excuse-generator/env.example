GROQ_API_KEY=your_groq_api_key_here

# "false" (default) — server key is used; users may optionally override it via the key button in the header.
# "true" (user mode) — server key is never used; users must supply their own key via the key button in the header.
NEXT_PUBLIC_USER_MODE=false
